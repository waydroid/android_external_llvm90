cc_defaults {
    name: "llvm90-lib-defaults",
    vendor_available: true,
    defaults: ["llvm90-defaults"],
    export_header_lib_headers: ["llvm90-headers"],
    target: {
        windows: {
            enabled: true,
        },
    },
}

subdirs = ["*"]
